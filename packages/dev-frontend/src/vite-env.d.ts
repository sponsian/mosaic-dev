/// <reference types="vite/client" />

// Compile-time constant injected by vite.config.ts. True only when
// VITE_APP_DEMO_MODE=true is set in the build environment.
declare const __MOSAIC_DEMO__: boolean;
