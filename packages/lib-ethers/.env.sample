# If you want to use `buidler deploy` on a testnet (e.g. `buidler deploy --network ropsten`)
# you need to pass a private key in env var DEPLOYER_PRIVATE_KEY. One way to do that is to
# copy this file to `.env` and put the key in there.
DEPLOYER_PRIVATE_KEY=0x...

# === CoinMarketCap Price Feed Updater (Reef Testnet) ===
# Get an API key at https://coinmarketcap.com/api/
CMC_API_KEY=your-api-key-here
# Address of deployed PriceFeedTestnet contract
PRICE_FEED_ADDRESS=0x...
# Reef testnet RPC endpoint
RPC_URL=https://rpc-testnet.reefscan.com
# Private key for the account that calls setPrice() (use a dedicated hot wallet)
UPDATER_PRIVATE_KEY=0x...
# Polling interval in milliseconds (default: 300000 = 5 minutes)
UPDATE_INTERVAL_MS=300000
# Max allowed price deviation percentage before skipping update (default: 50)
MAX_PRICE_DEVIATION_PERCENT=50
